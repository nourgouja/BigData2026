<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
<!--
  Gaza YouTube Analytics - Hadoop Core Site Configuration
  Defines HDFS NameNode URI and I/O settings
-->
<configuration>
    <!-- HDFS NameNode URI -->
    <property>
        <name>fs.defaultFS</name>
        <value>hdfs://namenode:9000</value>
        <description>NameNode URI for HDFS filesystem</description>
    </property>

    <!-- Hadoop temporary directory -->
    <property>
        <name>hadoop.tmp.dir</name>
        <value>/hadoop/tmp</value>
        <description>Base directory for temporary files</description>
    </property>

    <!-- I/O file buffer size -->
    <property>
        <name>io.file.buffer.size</name>
        <value>131072</value>
        <description>Buffer size for I/O operations (128 KB)</description>
    </property>

    <!-- Proxy user settings (for YARN) -->
    <property>
        <name>hadoop.proxyuser.root.hosts</name>
        <value>*</value>
    </property>

    <property>
        <name>hadoop.proxyuser.root.groups</name>
        <value>*</value>
    </property>

    <!-- HTTP authentication -->
    <property>
        <name>hadoop.http.authentication.simple.anonymous.allowed</name>
        <value>true</value>
    </property>

    <!-- Security -->
    <property>
        <name>hadoop.security.authentication</name>
        <value>simple</value>
    </property>

    <property>
        <name>hadoop.security.authorization</name>
        <value>false</value>
    </property>
</configuration>
